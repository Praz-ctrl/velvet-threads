// Smooth scroll animationsconst scrollLinks = document.querySelectorAll('a[href^="#"]');scrollLinks.forEach((link) => { link.addEventListener('click', (e) => { e.preventDefault(); const href = link.getAttribute('href'); const offsetTop = document.querySelector(href).offsetTop; window.scrollTo({ top: offsetTop, behavior: 'smooth' }); }); });// Parallax effectconst parallaxElements = document.querySelectorAll('.parallax');window.addEventListener('scroll', () => { parallaxElements.forEach((element) => { const offsetTop = element.offsetTop; const scrollTop = window.scrollY; const parallaxOffset = (scrollTop - offsetTop) * 0.1; element.style.transform = `translateY(${parallaxOffset}px)`; }); });// Floating cart iconconst floatingCart = document.querySelector('.floating-cart');window.addEventListener('scroll', () => { const scrollTop = window.scrollY; if (scrollTop > 200) { floatingCart.style.opacity = 1; } else { floatingCart.style.opacity = 0; } });// Hamburger menuconst hamburger = document.querySelector('.hamburger');const menu = document.querySelector('.menu');hamburger.addEventListener('click', () => { menu.style.display = 'block'; });menu.addEventListener('click', (e) => { if (e.target === menu) { menu.style.display = 'none'; } });